NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
ANTHROPIC_API_KEY=
# Optional — gates the demo setup route (/api/demo/setup) that seeds the Demo Kitchen
DEMO_SETUP_SECRET=
# Optional — gates the daily keepalive route (/api/keepalive) that stops the free-tier
# Supabase project being paused for inactivity. Vercel Cron sends this automatically as
# `Authorization: Bearer <value>`; use the same value for the GitHub CRON_SECRET secret.
CRON_SECRET=
